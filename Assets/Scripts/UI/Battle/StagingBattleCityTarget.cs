using UnityEngine;

namespace GlobalDomination.UI.Battle
{
    /// <summary>
    /// Marks colliders that represent the defender city for shoot raycasts and projectile hits.
    /// </summary>
    public sealed class StagingBattleCityTarget : MonoBehaviour
    {
    }
}
