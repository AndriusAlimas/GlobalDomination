using UnityEngine;

namespace GlobalDomination.UI.Battle
{
    /// <summary>
    /// Marks the staging battle ground plane for raycasts (move orders). Default layer is fine.
    /// </summary>
    public sealed class StagingBattleGround : MonoBehaviour
    {
    }
}
